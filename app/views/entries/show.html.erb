<h1>Word</h1>

<%= render 'entry', entry: @entry %>

<%= link_to 'Edit', edit_entry_path(@entry) %>

<%= link_to('Delete', entry_path(@entry), method: :delete, data: {confirm: "Are you sure you want to delete '#{@entry.word}' ?"})%>
